<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
    <meta name="theme-color" content="#ffffff" media="(prefers-color-scheme: light)">
    <meta name="theme-color" content="#1e1e1e" media="(prefers-color-scheme: dark)">
    <title>PlotBot Chat</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/github.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/github-dark.min.css" rel="stylesheet" media="(prefers-color-scheme: dark)">
    <link href="/css/style.css" rel="stylesheet">
<style>
:root {
  --vh: 100%;
}

html, body {
  height: 100%;
}

.chat-container {
  display: flex;
  flex-direction: column;
  height: calc(var(--vh, 1vh) * 95);
}

</style>

</head>
<body class="bg-light">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-8 mx-auto px-0 px-md-3">
                <div class="chat-container bg-white shadow-sm rounded-3 my-4">
                    <div class="chat-header p-3 border-bottom d-flex justify-content-between align-items-center">
                        <h4 class="mb-0">M&oslash;n Turistbot</h4>
                        
                        <div class="d-flex align-items-center gap-3">
                            <div id="connection-status" class="connection-status">
                                <span class="status-dot"></span>
                                <span class="status-text">Forbinder...</span>
                            </div>
                            <button id="retry-button" class="btn btn-outline-secondary btn-sm d-none">
                                <i class="bi bi-arrow-clockwise"></i>Pr&oslash;v igen 
                            </button>
                            <button id="theme-toggle" class="btn btn-outline-secondary btn-sm">
                                <i class="bi bi-sun-fill theme-icon-light"></i>
                                <i class="bi bi-moon-fill theme-icon-dark"></i>
                            </button>
                        </div>
                    </div>
                    
             <div class="chat-header p-3 border-bottom d-flex justify-content-between align-items-center">
                    Send mig et sp&oslash;rgsm&aring;l om M&oslash;n.
            </div>
                    <div class="chat-messages p-3" id="chat-messages">
                        <!-- Messages will be inserted here -->
                    </div>
                    <div class="chat-input border-top p-3">
                        <form id="chat-form" class="d-flex gap-2">
                            <input type="text" class="form-control" id="message-input" placeholder="Sp&oslash;rg l&oslash;s..." required>
                            <button type="submit" class="btn btn-primary d-flex align-items-center gap-2">
                                <span>Send</span>
                                <i class="bi bi-send"></i>
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script>
  const input = document.getElementById('message-input');
  input.addEventListener('focus', () => {
    setTimeout(() => {
      input.scrollIntoView({ behavior: 'smooth', block: 'center' });
    }, 300);
  });
</script>
<script>
  function setViewportHeight() {
    const vh = window.innerHeight * 0.01;
    document.documentElement.style.setProperty('--vh', `${vh}px`);
  }

  window.addEventListener('resize', setViewportHeight);
  window.addEventListener('orientationchange', setViewportHeight);
  window.addEventListener('load', setViewportHeight);
  setViewportHeight();
</script>    

<script>
  window.chatId = "<%= chatId %>";
  console.log("Using chatId:", window.chatId);
</script>

  
</script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>
    <script src="/js/theme.js"></script>
    <script src="/js/chat.js"></script>
</body>
</html> 